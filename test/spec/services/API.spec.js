describe("Pending A suite is just a function", function() {

    it("should instanziate jquery and plentyframework", function () {
    });

    it("and so is a spec", function() {
        var a = true;

        expect(a).toBe(true);
    });
});